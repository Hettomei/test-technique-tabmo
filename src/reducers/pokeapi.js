/*
 * State will look like
  {
    "bulbasaur": { // all agregatted data from pokeapi },
    "ivysaur":   { // all agregatted data from pokeapi },
  },
*/
export function pokeapi(state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
}
